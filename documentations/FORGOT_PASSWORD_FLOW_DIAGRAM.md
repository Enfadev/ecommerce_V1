# Forgot Password Flow Diagram

## User Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                     FORGOT PASSWORD FLOW                             │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│   User di    │
│  /signin     │
│   Page       │
└──────┬───────┘
       │
       │ Klik "Forgot password?"
       ▼
┌──────────────────┐
│  /forgot-password │
│      Page         │
│                   │
│  [Input Email]    │
│  [Send Button]    │
└──────┬────────────┘
       │
       │ Submit email
       ▼
┌───────────────────────────────────────┐
│  POST /api/auth/forgot-password       │
│                                       │
│  1. Validate email                    │
│  2. Check user exists                 │
│  3. Generate secure token             │
│  4. Save to database                  │
│  5. Send email via Resend             │
└───────┬───────────────────────────────┘
        │
        │ Email sent
        ▼
┌──────────────────┐         ┌────────────────────┐
│  Success Screen  │         │   Email Inbox      │
│                  │         │                    │
│  ✅ Email sent   │         │  📧 Reset Email    │
│  Check inbox     │         │                    │
└──────────────────┘         │  [Reset Password]  │
                             │      Button        │
                             └────────┬───────────┘
                                      │
                                      │ Click button
                                      ▼
                             ┌────────────────────┐
                             │ /reset-password    │
                             │   ?token=xxx       │
                             │                    │
                             │ 1. Validate token  │
                             │ 2. Check expired?  │
                             │ 3. Check used?     │
                             └────────┬───────────┘
                                      │
                    ┌─────────────────┴─────────────────┐
                    │ Valid?                            │
                    │                                   │
              Yes ┌─┴─┐                           No ┌─┴─┐
                  │   │                              │   │
                  ▼   │                              ▼   │
        ┌──────────────────┐              ┌──────────────────┐
        │  Reset Password  │              │  Error Screen    │
        │      Form        │              │                  │
        │                  │              │  ❌ Invalid or   │
        │ [New Password]   │              │  expired link    │
        │ [Confirm Pass]   │              │                  │
        │ [Reset Button]   │              │ [Request New]    │
        └────────┬─────────┘              └──────────────────┘
                 │
                 │ Submit new password
                 ▼
        ┌────────────────────────────┐
        │ POST /api/auth/            │
        │      reset-password        │
        │                            │
        │ 1. Validate token again    │
        │ 2. Check password strength │
        │ 3. Hash password           │
        │ 4. Update user password    │
        │ 5. Mark token as used      │
        └────────┬───────────────────┘
                 │
                 │ Success
                 ▼
        ┌──────────────────┐
        │  Success Screen  │
        │                  │
        │  ✅ Password     │
        │     reset!       │
        │                  │
        │  [Go to Sign In] │
        └────────┬─────────┘
                 │
                 │ Auto redirect after 3s
                 ▼
        ┌──────────────────┐
        │   /signin Page   │
        │                  │
        │ Sign in with     │
        │ new password     │
        └──────────────────┘
```

## Database Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    DATABASE OPERATIONS                       │
└─────────────────────────────────────────────────────────────┘

Request Reset:
┌─────────────┐    1. Find user    ┌──────────────┐
│   Request   │───────────────────→│    Users     │
│   Email     │                    │    Table     │
└─────────────┘                    └──────────────┘
      │
      │ 2. Delete old tokens
      ▼
┌─────────────────────────┐
│  PasswordResetTokens    │
│  DELETE old unused      │
└─────────────────────────┘
      │
      │ 3. Create new token
      ▼
┌─────────────────────────┐
│  PasswordResetTokens    │
│  INSERT new token       │
│  - email                │
│  - token (unique)       │
│  - expires (1 hour)     │
│  - used: false          │
└─────────────────────────┘

Reset Password:
┌─────────────┐    1. Find token   ┌─────────────────────────┐
│   Reset     │───────────────────→│  PasswordResetTokens    │
│  Request    │                    │  SELECT by token        │
└─────────────┘                    └─────────────────────────┘
      │                                     │
      │                                     │ 2. Validate
      │                            ┌────────┴─────────┐
      │                            │ Check:           │
      │                            │ - Not expired?   │
      │                            │ - Not used?      │
      │                            └────────┬─────────┘
      │                                     │ Valid
      │                                     ▼
      │ 3. Update password      ┌──────────────────┐
      └────────────────────────→│   Users Table    │
                                │   UPDATE password│
                                └──────────────────┘
                                         │
                                         │ 4. Mark token used
                                         ▼
                                ┌─────────────────────────┐
                                │  PasswordResetTokens    │
                                │  UPDATE used: true      │
                                └─────────────────────────┘
```

## Security Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURITY MEASURES                         │
└─────────────────────────────────────────────────────────────┘

Token Generation:
┌──────────────────┐
│  crypto.random   │
│    Bytes(32)     │──→ 64-char hex token (256-bit security)
└──────────────────┘

Token Storage:
┌─────────────────────────────────┐
│  Database                       │
│  - Unique constraint            │
│  - Index on email               │
│  - Expires timestamp            │
│  - Used boolean flag            │
└─────────────────────────────────┘

Token Validation:
┌─────────────────────────────────┐
│  Multi-layer checks:            │
│  1. Token exists in DB?         │
│  2. Not expired? (< 1 hour)     │
│  3. Not used before?            │
│  4. Email matches user?         │
└─────────────────────────────────┘

Password Security:
┌─────────────────────────────────┐
│  Before Storage:                │
│  1. Length validation (≥8)      │
│  2. Bcrypt hashing (10 rounds)  │
│  3. Store hash only             │
└─────────────────────────────────┘

Email Enumeration Protection:
┌─────────────────────────────────┐
│  Always return success:         │
│  "If account exists, email      │
│   will be sent"                 │
│  (Never reveal if email exists) │
└─────────────────────────────────┘
```

## Email Flow

```
┌─────────────────────────────────────────────────────────────┐
│                       EMAIL FLOW                             │
└─────────────────────────────────────────────────────────────┘

┌──────────────┐
│   Backend    │
│   API        │
└──────┬───────┘
       │ sendPasswordResetEmail()
       ▼
┌──────────────────────┐
│  Resend Email API    │
│  - API Key auth      │
│  - Rate limiting     │
│  - Delivery tracking │
└──────┬───────────────┘
       │
       │ SMTP
       ▼
┌──────────────────────┐
│  Email Provider      │
│  (Gmail, Outlook,    │
│   etc.)              │
└──────┬───────────────┘
       │
       │ Delivered
       ▼
┌──────────────────────┐
│  User's Inbox        │
│                      │
│  📧 Beautiful HTML   │
│     email with:      │
│  - Company logo      │
│  - Gradient design   │
│  - Reset button      │
│  - Fallback link     │
│  - Security notes    │
└──────────────────────┘
```

## Component Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                  COMPONENT STRUCTURE                         │
└─────────────────────────────────────────────────────────────┘

Frontend Components:
├── app/
│   ├── (auth)/
│   │   ├── forgot-password/
│   │   │   └── page.tsx ──────────┐
│   │   │       │                   │
│   │   │       ├── EmailInput      │
│   │   │       ├── SubmitButton    │
│   │   │       └── SuccessScreen   │
│   │   │                           │
│   │   └── reset-password/         │
│   │       └── page.tsx ────────┐  │
│   │           │                │  │
│   │           ├── TokenValidation  │
│   │           ├── PasswordInputs   │
│   │           ├── SubmitButton     │
│   │           ├── SuccessScreen    │
│   │           └── ErrorScreen      │
│   │                           │  │
│   └── api/                   │  │
│       └── auth/              │  │
│           ├── forgot-password/  │
│           │   └── route.ts ──┼──┘
│           │       │           │
│           │       └── POST endpoint
│           │                   │
│           └── reset-password/ │
│               └── route.ts ───┘
│                   │
│                   ├── GET (validate)
│                   └── POST (reset)
│
└── lib/
    └── email.ts
        │
        └── sendPasswordResetEmail()
            │
            ├── HTML template
            └── Resend integration
```

---

## Legend

```
┌─────┐
│ Box │  = Component or Step
└─────┘

   │     = Flow direction
   ▼

   ├─    = Decision or Branch
   │

  ───→   = Data flow

  ✅     = Success state
  ❌     = Error state
  📧     = Email related
  🔒     = Security related
```
