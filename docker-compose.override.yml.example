# docker-compose.override.yml
# File ini untuk kustomisasi lokal (tidak di-commit ke git)
# Copy file ini dan rename menjadi docker-compose.override.yml untuk menggunakannya

# Contoh: Override port
# services:
#   app:
#     ports:
#       - "3001:3000"
#   db:
#     ports:
#       - "3307:3306"

# Contoh: Tambah environment variables
# services:
#   app:
#     environment:
#       - DEBUG=true
#       - LOG_LEVEL=debug

# Contoh: Mount additional volumes
# services:
#   app:
#     volumes:
#       - ./custom-config:/app/config

# Contoh: Override command
# services:
#   app:
#     command: npm run dev -- --hostname 0.0.0.0 --port 3000
